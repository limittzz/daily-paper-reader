---
title: "WorldDiT: A Unified Diffusion Architecture for World and Action Modeling"
title_zh: WorldDiT：统一的世界与动作建模扩散架构
authors: "Sen Wang, R. Gnana Praveen, Bidhan Roy, Marcos Villagra"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.23909v1"
tags: ["query:imitation"]
score: 8.0
evidence: 用于机器人动作生成与世界建模的统一扩散变换器
tldr: 针对机器人策略依赖大型预训练视觉语言模型导致的计算开销问题，提出WorldDiT扩散Transformer架构，统一联合生成连续动作块与预测未来相机帧的归一化RGB块。在LIBERO四个仿真套件上，该方法以亚十亿参数在模型总参数量和平均成功率上处于帕累托前沿，为后续扩展研究提供了强基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23909v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1614, \"height\": 1192, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1683, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23909v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 670, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23909v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1730, \"height\": 1165, \"label\": \"Table\"}]"
motivation: 现有机器人策略常依赖大型预训练VLM作为动作主干，计算成本高，且独立于世界建模。
method: 提出统一扩散Transformer，同时生成连续动作块并预测未来帧的RGB块，无需大VLM主干。
result: 在LIBERO四个套件上，以亚十亿参数达到帕累托前沿，兼顾小参数量与高成功率。
conclusion: WorldDiT为亚十亿参数规模的机器人策略研究提供了强基线，并促进世界建模与动作生成的联合学习。
---

## 摘要
近期的许多机器人策略通过使用大型预训练视觉语言模型（VLM）作为动作主干来追求更强的控制能力。我们提出了WorldDiT，一种统一的扩散变换器架构，它将动作生成与视觉世界建模相结合，无需大型预训练VLM动作主干即可实现强大性能。在训练过程中，单个扩散变换器生成连续的动作块，并从未来相机帧中预测归一化的RGB块目标。在四个LIBERO仿真套件中，WorldDiT位于报告的总模型参数与平均成功率之间的帕累托前沿上。这些结果为未来的扩展研究提供了一个强大的亚十亿参数基线。

## Abstract
Many recent robot policies pursue stronger control by using large pretrained vision-language models (VLMs) as the action backbone. We introduce WorldDiT, a unified diffusion transformer architecture that couples action generation with visual world modeling and achieves strong performance without a large pretrained VLM action backbone. During training, a single diffusion transformer generates continuous action chunks and predicts normalized RGB patch targets from future camera frames. Across four LIBERO simulation suites, WorldDiT lies on the reported Pareto frontier for total model parameters and mean success among methods reporting all four suites. These results provide a strong sub-billion-parameter baseline for future scaling studies.

---

## 论文详细总结（自动生成）

# WorldDiT 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题背景**：当前许多机器人策略依赖大型预训练视觉-语言模型（VLM）作为动作主干，带来了显著的参数量和计算开销，同时难以区分“模型规模”与“架构设计”对性能的贡献。
- **核心动机**：探索是否可以在**不使用大型预训练VLM**作为动作主干的前提下，通过**统一扩散变换器架构**同时学习动作生成与未来视觉预测，从而在较低参数量下达到有竞争力的控制性能。
- **整体含义**：WorldDiT 验证了“世界建模”与“动作生成”可以紧凑地结合在一个共享骨干中，为后续亚十亿参数级别的机器人学习扩展研究提供了一个强基线。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：采用单个扩散变换器（DiT）骨干，同时对**连续动作块**和**未来归一化RGB块**进行建模，利用辅助的未来视觉预测目标增强动作生成，而推理时仅使用动作分支。
- **关键技术细节**：
  - **输入编码**：冻结的MAE图像编码器（+共享Perceiver Resampler）和CLIP文本编码器处理多视角RGB和语言指令；可训练的机器人状态编码器处理机械臂和夹爪状态。
  - **目标构建**：
    - 动作目标：7步连续动作块 \( \mathbf{A}_i \in \mathbb{R}^{7 \times 7} \)。
    - 未来RGB块目标：从未来主相机和腕相机帧中提取 \( 16 \times 16 \) 块，每相机均匀保留64个归一化后（均值、方差归一化）的768维向量，共128个目标。
  - **训练流程**：给定3个观察上下文步骤，使用**流匹配（Flow Matching）** 目标，对噪声化的动作和RGB块预测其速度场。总损失为动作损失与RGB块损失的加权和（权重分别为0.1和0.001）。
  - **推理流程**：仅使用动作路径，从高斯噪声开始，通过20步Euler积分生成动作块，执行前3个动作后基于新观测重规划（滚动时域控制）。
- **骨干结构**：DiT深度=4，隐藏维度=1024，16个注意力头，4个寄存器token，总参数量399M（含冻结模块） / 135M可训练。

## 3. 实验设计：数据集、基准与方法对比
- **数据集/场景**：使用 **LIBERO** 仿真基准，包含四个下游套件：`libero_spatial`（空间）、`libero_object`（物体）、`libero_goal`（目标）、`libero_10`（长时任务，记为LIBERO Long）。另外使用 `libero_90` 多任务拆分进行预训练。
- **基准**：对比24种已发表方法，按是否使用大型预训练VLM动作主干分为两组。主要评价指标为各套件成功率及平均成功率。
- **对比方法示例**：
  - 使用大型VLM：ACoT VLA (98.5%), MMaDA VLA (98.0%), VLANeXt (97.4%), π0.5 (96.9%) 等。
  - 不使用大型VLM：DreamVLA (92.6%), FlowVLA (88.1%), DiT Policy (82.4%), Octo (75.1%) 等。
- **WorldDiT结果**：Spatial 98.0%, Object 97.0%, Goal 92.8%, Long 91.8%，平均 **94.9%**。在所有方法中处于帕累托前沿（参数量—成功率权衡）。

## 4. 资源与算力
- **训练硬件**：单个节点，配备 **8块 RTX Pro 6000 GPU**。
- **训练设置**：
  - 在 `libero_90` 上预训练30个epoch，per-GPU batch size=40，梯度累积2，有效batch size=80，学习率1e-4（余弦调度，1个warmup epoch）。
  - 在每个下游套件上独立微调，per-GPU batch size=32，梯度累积2，8块GPU有效batch size=512，学习率1e-4，使用bf16混合精度。
- **训练时长**：论文未明确给出总训练时间或微调轮数，仅提及预训练30 epoch。

## 5. 实验数量与充分性
- **实验数量**：每个套件评估 **500个仿真回合**，其中300回合用于阶段性检查点选择，因此最终报告分数并非完全独立测试集估计。
- **实验充分性**：
  - 覆盖了LIBERO全部四个标准套件，对比了24种主流方法，具有一定覆盖面。
  - 但缺乏以下关键验证：
    - **消融实验**：未单独分析未来RGB目标的作用、损失权重的影响、上下文长度或动作块长度选择等。
    - **跨benchmark验证**：仅在LIBERO上评估，未在其他仿真或真实机器人场景上测试。
    - **多次运行统计**：未报告多次随机种子下的均值和方差。
- **客观性**：由于对比方法的结果来自原文报告，且部分方法的参数量为重构近似，可能存在协议不一致。作者明确声明结果不应视为无偏测试估计。

## 6. 主要结论与发现
- **核心结论**：一个统一扩散变换器可以在不使用大型预训练VLM主干的前提下，通过联合动作生成与未来视觉预测训练，在LIBERO上达到与百亿参数级方法相当的竞争力（平均94.9%成功率）。
- **关键发现**：WorldDiT以399M总参数量位于所报告方法的帕累托前沿——所有成功率高于它的方法参数量更大，所有参数量等于或小于它的方法成功率更低。这为亚十亿参数机器人学习提供了可行的设计路径。

## 7. 优点
- **架构简洁高效**：不使用大型VLM，参数量仅399M，可部署于中等算力硬件。
- **统一建模**：动作生成与视觉世界建模共享DiT骨干，训练时辅助未来块预测，推理时仅动作分支，无额外开销。
- **强基准价值**：在LIBERO上为后续扩展研究提供了清晰的基线点，有利于分离规模与架构贡献。
- **帕累托前沿位置**：直观展示了参数量-性能的折中，对资源受限场景具有参考意义。

## 8. 不足与局限
- **实验覆盖有限**：仅在LIBERO仿真套件上评估，缺乏真实机器人或其他仿真环境（如Bridge、RLBench）的泛化验证。
- **评估偏差风险**：500回合中包含300回合用于检查点选择，导致报告分数偏高且非无偏估计，可能影响与方法对比的公平性。
- **缺乏消融与分析**：未进行关键组件消融（如是否使用未来RGB目标、损失权重、预测步数等），也未提供多次运行统计，难以判断方法的稳健性。
- **可复现性问题**：论文未提供完整训练代码和模型权重，对比方法中部分参数量为近似，降低结果可复现程度。
- **应用限制**：当前配置仅在特定任务集（LIBERO）上验证，且动作空间维度较低（7维），在更复杂、高维控制任务上的表现未知。

（完）

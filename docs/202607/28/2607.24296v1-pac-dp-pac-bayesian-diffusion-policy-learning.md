---
title: "PAC-DP: PAC-Bayesian Diffusion Policy Learning"
title_zh: "PAC-DP: PAC-贝叶斯扩散策略学习"
authors: "Mohammad Hasan Yeganegi, Dian Yu, Andrea Del Prete, Majid Khadiv, Matteo Saveriano"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24296v1"
tags: ["query:imitation"]
score: 9.0
evidence: 用于机器人操作的扩散策略的PAC-Bayesian正则化
tldr: 扩散策略在复杂机器人操作任务中表现突出，但通过最小化去噪目标训练，缺乏对有限数据泛化的控制。本文提出PAC-DP，将扩散策略视为贝叶斯神经网络，推导PAC-Bayes泛化界，得到包含KL散度正则化的新训练损失。该方法在不显著增加训练时间的前提下，提升去噪性能、降低变分负对数似然，并在多个机器人操作基准上获得更高成功率。特别是在低数据训练和复杂任务场景中改进幅度最大。PAC-DP为机器人策略学习提供了理论严谨且实用的正则化框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24296v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1510, \"height\": 919, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1863, \"height\": 304, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1887, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24296v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 919, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24296v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1867, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1870, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 673, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24296v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 910, \"height\": 245, \"label\": \"Table\"}]"
motivation: 在机器人操作任务中，扩散策略在有限数据下泛化能力不足，需用正则化方法提升性能。
method: 将扩散策略建模为贝叶斯神经网络，并使用PAC-Bayes泛化界引入KL散度正则化项来约束参数分布。
result: 在多个机器人操作基准上，该方法提升了去噪性能，降低了负对数似然，并提高了任务成功率。
conclusion: PAC-DP为机器人策略学习提供了理论正则化框架，尤其适用于低数据场景和复杂任务。
---

## 摘要
扩散策略能够执行复杂的操作任务。然而，DP通常通过最小化去噪目标进行训练，这在机器人学常见的有限数据场景中对泛化性的控制有限。本文提出PAC-DP，一种提升DP在机器人操作任务中性能的方法。通过将DP建模为贝叶斯神经网络，并定义PAC-贝叶斯泛化界，我们推导出一种新的训练目标，该目标在标准去噪损失的基础上增加了后验与先验参数分布之间的KL散度正则化项。从理论角度看，我们的方法提供了一种正则化DP训练的原则性途径，且不会显著增加训练时间。从实践角度看，实验结果表明，在多个机器人操作基准测试中，该方法提升了去噪性能，降低了变分负对数似然，并提高了成功率。关键的是，在低数据训练场景和复杂任务中观察到最大改进，使PAC-DP成为一种理论完备的机器人策略学习框架。

## Abstract
Diffusion Policies (DPs) are able to perform complex manipulation tasks. However, DPs are typically trained by minimizing a denoising objective, which provides limited control over generalization in the finite-data regimes common in robotics. In this letter, we propose PAC-DP, an approach that increases the performance of DPs in robotic manipulation tasks. By modeling the DP as a Bayesian neural network, and defining a PAC-Bayes generalization bound, we derive a novel training objective that augments the standard denoising loss with a Kullback-Leibler divergence regularizer between the posterior and prior parameter distributions. From the theoretical perspective, our approach provides a principled approach to regularize the training of DPs without significantly increasing the training time. From the practical point of view, experimental results demonstrate improved denoising performance, lower variational negative log-likelihood, and higher success rates across multiple robotic manipulation benchmarks. Crucially, the largest improvements are observed in low-data training regimes and complex tasks, establishing PAC-DP as a theoretically grounded framework for robot policy learning.

---

## 论文详细总结（自动生成）

# 论文《PAC-DP: PAC-Bayesian Diffusion Policy Learning》详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：扩散策略（Diffusion Policy, DP）通过迭代去噪生成多模态动作分布，在机器人模仿学习中表现优异。然而，DP的标准训练仅最小化去噪损失（公式2），在机器人领域常见的有限数据场景下，该损失无法提供良好的泛化保证。已有理论表明，仅优化经验去噪目标可能导致模型记忆训练样本，而非学习真实数据分布。
- **核心问题**：如何设计一种理论上有泛化保证的训练目标，使DP在低数据场景下仍能保持高成功率和良好的生成质量。
- **整体含义**：本文提出 **PAC-DP**，将PAC-Bayesian学习理论引入扩散策略训练，通过贝叶斯神经网络参数化去噪网络，在标准去噪损失上增加KL散度正则项，从而鼓励后验分布接近先验，实现原则性的正则化，在不增加显著计算开销的前提下提升泛化能力。

## 2. 方法论
### 核心思想
- 将DP中的噪声预测网络视为一个随机预测器，其参数服从后验分布 $Q(w)$。根据PAC-Bayes-kl不等式（公式5），期望风险受经验风险和KL散度共同控制。
- 通过最小化由PAC-Bayes界导出的代理损失（公式10），同时降低训练损失和后验-先验KL散度，实现理论驱动的正则化。

### 关键技术细节
1. **贝叶斯神经网络**：去噪网络 $\epsilon_w(x_t, t, o)$ 的所有层（除条件投影层外）均采用因子化高斯后验 $Q(w) = \mathcal{N}(\mu, \text{diag}(\sigma^2))$，使用重参数化技巧采样 $w = \mu + \sigma \odot \xi$，其中 $\sigma = \log(1 + e^\rho)$ 以确保非负。
2. **先验设定**：先验 $P$ 为因子化零均值截断高斯分布。
3. **PAC-DP训练目标**（公式10）：
   $$L_{\text{PAC-DP}} = \hat{R}(Q) + \sqrt{\frac{2\hat{R}(Q)(\lambda D_{KL}(Q\|P) + \ln\frac{2\sqrt{N}}{\delta})}{N}} + \frac{2(\lambda D_{KL}(Q\|P) + \ln\frac{2\sqrt{N}}{\delta})}{N}$$
   其中 $\hat{R}(Q) = \mathbb{E}_{w\sim Q}[L_{DP}(w)]$ 是经验去噪损失，$\lambda$ 控制正则化强度，$N$ 为训练样本数，$\delta$ 为置信度参数（实际设为固定小值）。
4. **推理**：可采样后验参数或使用后验均值 $\mu$（确定性推理）。采用DDIM加速采样，仅用10步去噪。

### 算法流程（文字说明）
- **训练**（算法1）：每次迭代，采样干净动作 $x_0$、观测 $o$、时间步 $t$ 和噪声 $\epsilon$，加噪得到 $x_t$；从后验 $Q$ 中采样网络参数 $w$；前向计算噪声预测 $\hat{\epsilon}$；计算PAC-DP损失并反向传播更新后验参数 $\mu, \rho$。
- **推理**（算法2）：选择推理模式（均值或采样），初始化 $x_T\sim\mathcal{N}(0,I)$，从 $T$ 到 $1$ 循环，使用DDIM调度器逐步去噪。

## 3. 实验设计
### 数据集/场景
- **模拟环境**：PushT（从[2]引入）、Robomimic基准中的五个任务：Lift、Can、Square、Transport、ToolHang。均使用基于状态（state-based）的观测和成功专家演示。
- **低数据场景**：使用20、40、80、200个演示（Transport和ToolHang仅用200个，因基线在更少数据下无法可靠学习）。
- **真实机器人**：Franka Emika Panda，执行四个任务（Lift、Can、Square、ToolHang）。

### Benchmark
- 标准DP [2]（确定性网络，标准去噪损失）。
- 贝叶斯DP（BDP）：使用贝叶斯神经网络但仅用去噪损失，无PAC-Bayes正则化，作为消融对照。

### 评价指标
- **噪声预测误差（NPE）**：在测试集上的均方误差。
- **变分负对数似然下界（NLL下界）**：采用[32]的信息论方法评估生成分布质量。
- **任务成功率（SR）**：每个任务1000个随机初始化环境，每个种子共30000次 rollout，取最后10个检查点的平均。

### 实验数量与充分性
| 实验类型 | 数量/覆盖 |
|---------|-----------|
| 模拟任务 | 6个任务（PushT + 5 Robomimic） |
| 数据量 | 4种（20,40,80,200），部分任务仅200 |
| 超参数消融 | λ ∈ {10⁻⁵,10⁻⁶}，ρ ∈ {-3.5,-4.0,-4.5,-5.0}，共8种组合每个任务 |
| 贝叶斯网络架构消融 | Full BN vs. Ours（仅去噪网络为BN），在ToolHang上对比 |
| 真实机器人 | 4个任务 × 5条轨迹 × 10次执行 = 200次执行 |
| 所有实验均采用3个随机种子，报告均值±标准差 | |

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量及具体训练时长。仅提及训练步数：PushT训练10⁶步，Robomimic中Lift/Can/Square训练2.5×10⁵步，Transport/ToolHang训练5×10⁵步。
- 由于作者单位（Trento大学、TUM）通常使用标准GPU（如NVIDIA RTX系列），但论文未提供详细硬件信息。

## 5. 实验充分性与公平性评估
- **充分性**：覆盖多种任务复杂度、多种数据规模、超参数敏感性、架构消融、真实机器人验证，实验较为全面。
- **公平性**：与两个强基线（DP、BDP）在同一架构和环境下对比，超参数通过消融选取，结果报告均值和标准差，遵循标准化评估流程。
- **潜在偏差**：大部分模拟任务在200个演示下基线已接近饱和（如Lift、Can），PAC-DP的优势主要体现在低数据或高复杂度任务；真实实验仅200次执行，统计稳定性有限；未考虑视觉观测输入（仅state-based），限制了方法在更广泛场景的验证。

## 6. 主要结论与发现
1. **PAC-DP显著提升去噪性能**：在PushT上NPE降低约67%（0.060 vs 0.184），NLL下界从约-3.24降至-8.275，表明模型更好地捕捉了数据分布。
2. **任务成功率提升**：尤其在低数据场景（20个演示）和复杂任务（ToolHang，成功率从40.1%升至73.8%）。
3. **正则化比贝叶斯参数化更重要**：BDP（仅贝叶斯无正则）在大多数任务上仅略优于DP，而PAC-DP进一步大幅提升，证明KL散度正则项是关键。
4. **超参数鲁棒性良好**：在大部分任务上平均性能接近最佳配置，尤其在Transport任务上PAC-DP的变异性显著低于BDP。
5. **真实机器人可部署**：200次真实执行全部成功，证明方法可迁移到物理硬件。

## 7. 优点
- **理论驱动**：直接基于PAC-Bayes泛化界设计损失，为扩散策略提供了统计学习理论基础。
- **无需修改架构**：只需将原有网络转换为贝叶斯版本，不改变U-Net等骨干结构，易集成。
- **低数据场景显著有效**：在20个演示时，多个任务成功率提升10%以上。
- **计算开销小**：仅增加KL散度计算（闭式），不显著增加训练时间。
- **消融实验严谨**：分离了贝叶斯参数化和PAC-Bayes正则化的效果，证明正则化是关键。
- **真实世界验证**：证明了仿真到实物的直接迁移能力。

## 8. 不足与局限
- **实验覆盖不足**：仅测试了基于状态的观测，未考虑图像/视觉观测；部分任务（Square、Transport）在200样本时成功率未提升甚至略有下降（但差异小），说明NPE降低并不总是转化为任务成功。
- **理论近似**：PAC-Bayes界要求有界损失，但实际使用无界MSE损失。作者论证在训练后期损失<1，近似合理，但严格的保证仍需有界版本。
- **真实机器人实验规模小**：仅200次执行，且为开环执行（无在线反馈），结果可能存在偶然性。
- **超参数调参**：λ和ρ仍需针对任务调整（λ∈{10⁻⁵,10⁻⁶}，ρ在-3.5~-5.0），虽然总体鲁棒但需一定搜索。
- **未考虑多任务/大规模预训练策略**：如Octo、OpenVLA等，方法未与这些系统结合对比。
- **开源与复现**：论文未提及代码或模型权重是否公开，影响可复现性。

（完）

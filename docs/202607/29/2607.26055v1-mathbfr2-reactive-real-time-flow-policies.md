---
title: "$π\\mathbf{R}^2$: Reactive Real-time Flow Policies"
title_zh: "$\\pi\\mathbf{R}^2$：反应式实时流策略"
authors: "Sungjae Park, Shubham Tulsiani"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26055v1"
tags: ["query:imitation"]
score: 9.0
evidence: 用于机器人控制的反应式实时扩散流策略
tldr: "通用操作策略常采用动作分块流策略，但因开环运行无法响应中间感官输入，且重新规划延迟高，不适合动态闭环控制。π𝐑²方法通过快慢双通道分离本体感受与视觉特征，并利用延迟自适应流调度实现单步去噪输出动作，使预训练大模型策略在真实平台上达到约25Hz闭环重规划，较基线快约4倍，模拟和真实世界成功率分别提升23%和30%。无需大幅修改架构即可微调，实现实时反应性闭环控制。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26055v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1037, \"height\": 731, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26055v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1383, \"height\": 354, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26055v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1399, \"height\": 450, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26055v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1380, \"height\": 304, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26055v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1378, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26055v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1397, \"height\": 367, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26055v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 474, \"height\": 337, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26055v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1366, \"height\": 2272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26055v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1428, \"height\": 317, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26055v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1057, \"height\": 734, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26055v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1199, \"height\": 275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26055v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1291, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26055v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1529, \"height\": 840, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26055v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1454, \"height\": 889, \"label\": \"Table\"}]"
motivation: 解决动作分块流策略开环运行导致反应性差和延迟高的问题，使其适用于动态闭环控制任务。
method: 利用扩散强制位置噪声调度，设计快慢双通道和延迟自适应流调度，实现单步去噪反应性动作生成。
result: "在xArm6+XHand平台上实现25Hz闭环重规划，成功率提升23%-30%，显著优于基线。"
conclusion: 轻量级微调方法使预训练大模型策略具备实时反应能力，提升动态任务性能。
---

## 摘要
通用操作策略越来越多地采用基于大型预训练骨干构建的动作分块流策略。此类分块以开环方式运行，因此策略无法对执行过程中到达的感官输入做出反应，牺牲了反应性。更频繁地重新规划可以恢复反应性，但感知-动作流水线（一个大型骨干加上多个去噪步骤）速度太慢：这种延迟禁止频繁重新规划，并使已提交的动作变得过时，使得此类策略不适用于动态闭环控制。我们提出了$\pi\mathbf{R}^2$，它使这些策略具有反应性和实时性，同时保留了大型骨干、表达性多模态策略和多动作预测。基于扩散强迫的逐位置噪声调度，$\pi\mathbf{R}^2$贡献了两个想法。首先，它将条件分解为快速通道（本体感觉，每个节拍更新）和异步更新的慢速通道（视觉-语言特征），因此策略可以在一个分块内对本体感觉做出反应，同时容忍过时的视觉信息。其次，一种延迟自适应流调度将正在执行的动作视为修补条件，并在每次调用时通过一个去噪步骤发出动作，让一个训练好的模型适应不同的硬件延迟。$\pi\mathbf{R}^2$对现有架构的修改要求极小，可以从预训练策略中微调：应用于真实xArm6+XHand平台上的GR00T-N1.7，它闭环重新规划的速度大约是基础策略的4倍（在A5000 GPU上约25Hz），每40ms基于新的观测采取行动。在模拟和真实世界操作任务中，$\pi\mathbf{R}^2$在模拟中将成功率提高了最多23%，在真实世界中比最强基线提高了30%。项目页面：https://pi-r2-flow.github.io/

## Abstract
Generalist manipulation policies increasingly take the form of action-chunking flow policies built on large pretrained backbones. Such chunks run open-loop, so the policy cannot react to sensory input arriving mid-execution, sacrificing \emph{reactivity}. Replanning more often would restore it, but the perception-to-action pipeline (a large backbone plus multiple denoising steps) is too slow: this \emph{latency} forbids frequent replanning and leaves committed actions stale, making such policies ill-suited for dynamic, closed-loop control. We present $π\mathbf{R}^2$, which makes these policies reactive and real-time while retaining large backbones, expressive multi-modal policies, and multi-action prediction. Built on the per-position noise schedule of diffusion forcing, $π\mathbf{R}^2$ contributes two ideas. First, it splits conditioning into a fast channel (proprioception, fresh every tick) and an asynchronously updated slow channel (vision-language features), so the policy reacts to proprioception within a chunk while tolerating stale vision. Second, a latency-adaptive flow schedule treats in-flight actions as inpainting conditioning and emits actions in one denoising step per call, letting one trained model adapt to varying hardware latency. Requiring minimal modification to existing architectures, $π\mathbf{R}^2$ can be finetuned from a pretrained policy: applied to GR00T-N1.7 on a real xArm6+XHand platform, it replans closed-loop roughly $4\times$ faster than the base policy (~$25$Hz on an A5000 GPU), acting on a fresh observation every $40$ms. Across simulation and real-world manipulation tasks, $π\mathbf{R}^2$ improves the success rate by up to $23\%$ in simulation and $30\%$ in the real world over the strongest baseline. Project page: https://pi-r2-flow.github.io/

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：当前通用操作策略普遍采用“动作分块流策略”（action-chunking flow policies），其核心组件包括大型预训练骨干（如VLM）、表达性多模态策略（扩散/流匹配）和动作分块预测。然而，这种设计导致**两个固有缺陷**：
  - **反应性差**：预测的动作块以开环方式执行，执行过程中无法对实时到达的感官输入（如本体感觉、视觉）做出反应。
  - **延迟高**：感知到动作的流水线（大型骨干+多步去噪）计算耗时巨大，导致重新规划频率极低（例如约7 Hz），预测的动作基于“过时”的观测。
- **意义**：这种策略不适合动态、接触丰富的操作任务，亟需一种方法在保留大模型、多模态和分块预测优势的同时，实现**实时的闭环反应式控制**。

## 2. 方法论：核心思想、关键技术细节、算法流程
- **核心思想**：基于**扩散强制（Diffusion Forcing）**的逐位置噪声调度，对现有流策略进行两项关键改进，使其具有实时反应性。
- **关键技术细节**：
  1. **快慢双通道条件分离**：
     - 将去噪动作头的条件分解为**快通道**（本体感受：关节位置、速度、力矩等，每个控制周期可低延迟获取）和**慢通道**（视觉-语言特征，由VLM异步处理，更新频率低）。
     - 训练时对慢通道施加随机延迟（\(d_{\text{vlm}} \sim \text{Uniform}\)），并学习延迟嵌入；推理时使用实测延迟。
     - 效果：动作头每步去噪都能基于最新本体感觉快速反应，同时容忍视觉特征的过时。
  2. **延迟自适应流调度**：
     - 设计三区域“阶梯”噪声调度 \(\tau^{\star,d}\)（前 \(d\) 个位置为纯净噪声（作为修补条件），中间线性下降，尾部 \(d\) 个纯噪声）。
     - 每次调用仅执行**一步欧拉去噪**，将缓冲区向右滑动 \(d\) 个位置，从而根据实际测量延迟 \(d\) 自适应调整。
     - 训练时随机采样 \(d\)，混合少量标准流训练，使得同一模型可适应不同硬件延迟。
- **算法流程**（以文字说明）：
  - **训练**：采样延迟 \(d\)，构建阶梯调度；前 \(d\) 个位置用真实动作填充并屏蔽损失；慢通道附加随机延迟；以 MSE 损失训练。
  - **推理**：启动时用标准流初始化缓冲区。此后每个控制周期：测量当前推理延迟 \(d\)，用一步去噪更新缓冲区，发出 \(d\) 个干净动作，滑动缓冲区并填入新噪声。

## 3. 实验设计：数据集/场景、基准、对比方法
- **模拟实验**：
  - **场景**：MuJoCo Playground 中的 Leap Cube Reorientation 任务（要求重定向立方体到指定姿态）。
  - **数据**：4个PPO专家生成200条轨迹，50 Hz。
  - **对比方法**：
    - 标准流匹配（不同执行步数 \(h\)）
    - 朴素异步流（Naive Async）
    - 训练时RTC（Train-Time RTC）
    - \(\pi\mathbf{R}^2\) 无异步处理 vs 有异步处理
  - **评价指标**：成功率（100 episodes）。
- **真实世界实验**：
  - **平台**：xArm6 + XHand，单目RGB相机，25 Hz控制率。
  - **任务**：4个接触丰富的灵巧操作任务：Don't Spill（移球入碗）、Tidy Up Book（取书放篮）、Insert Box（推盒插架）、Catch Book（接书）。
  - **数据**：每任务分别200/300/300/100条遥操作演示。
  - **对比方法**：
    - Flow, Synchronous（同步推理，执行 \(h=10\)）
    - Flow, Naive Async + TE（异步+时序集成）
    - Flow, Train-Time RTC
    - \(\pi\mathbf{R}^2\)（本文方法）
  - **评价指标**：成功率（SR，N=20 trials）和子目标完成进度（Prog）。

## 4. 资源与算力
- **模拟实验**：使用 1×A5000 GPU，训练预算800 epochs。
- **真实世界实验**：
  - 微调 GR00T-N1.7（Qwen-VL骨架+Eagle-2视觉编码器冻结，仅训练动作头）。
  - **训练**：8×RTX A5000/A6000（24/48 GB VRAM），每个任务分别训练约40k/28k/49k/4.55k步（对应200/100/100/100 epochs）。
  - **推理**：基线使用单个A5000；\(\pi\mathbf{R}^2\) 使用2个A5000（一个用于VLM异步线程，一个用于动作头线程），以避免计算争用。
  - **控制率**：25 Hz（1 tick ≈ 40 ms）。基线延迟 \(d=4\sim5\) ticks（~160-200 ms），\(\pi\mathbf{R}^2\) 延迟 \(d=1\sim2\) ticks（~40-80 ms）。

## 5. 实验数量与充分性
- **模拟实验**：
  - 执行步长扫描（\(h=1,2,4,8\)）并对比 \(\pi\mathbf{R}^2\)（1步）。
  - 延迟部署研究：固定延迟预算，对比4种方法，覆盖 \(d_0=1,2,3\)，报告3个训练种子的均值和标准差。
  - 消融：\(\pi\mathbf{R}^2\) 无异步 vs 有异步。
- **真实世界实验**：
  - 4个任务，每个任务20次试验，报告成功率和进度分数。
  - 额外提供反应性分析（指尖力曲线对比）。
- **充分性评价**：
  - **公正性**：所有方法基于相同预训练模型、相同数据集、相同训练预算仅改动调度和推理方式。
  - **充分性**：模拟实验覆盖不同延迟和消融，真实世界实验覆盖4个多样化的动态任务，并有定量和定性分析。但未在多样化的基础模型（如π0）上验证，仅限于GR00T-N1.7。
  - **客观性**：报告均值和标准差，未使用p值统计检验，但通过多次随机种子和多次试验提供统计支持。

## 6. 主要结论与发现
- \(\pi\mathbf{R}^2\) 在模拟和真实世界中**显著优于所有基线**：
  - 模拟中，成功率提升最高达23%（相比训练时RTC）。
  - 真实世界中，绝对成功率提升约20%~30%（例如Catch Book从25%到55%）。
- **关键机制**：
  - 快慢通道分离使策略能在每个去噪步使用最新本体感觉，实现4倍更高的重规划频率（~25 Hz vs ~7 Hz）。
  - 延迟自适应调度保证了动作平滑过渡，避免边界跳跃。
  - 反应性分析显示，\(\pi\mathbf{R}^2\) 能即时调节握力（~50 N），而基线因过时计划导致过度用力（~120 N）导致失败。
- **方法轻量**：仅需微调动作头（替换AdaLN为逐位置版本），不修改骨干。

## 7. 优点：方法与实验设计亮点
- **方法亮点**：
  - **实用性强**：对现有VLA架构改动极小（仅动作头噪声调制从共享变为逐位置），可从预训练策略直接微调。
  - **理论创新**：快慢通道分离利用了本体感觉成本低的特性，延迟自适应调度统一处理推理和通信延迟。
  - **实时性**：单步去噪+异步VLM，实现接近控制周期的反应频率。
- **实验亮点**：
  - 模拟实验定量解耦了执行步长和延迟的影响，验证了逐分量设计价值。
  - 真实世界任务选择多样化，覆盖力接触、动态抓取、非夹持操作等，且提供力曲线直观展示反应性差异。
  - 公开项目页面和代码（暗示可复现）。

## 8. 不足与局限
- **未解决外部延迟**：模型外部的通信延迟（如推理服务器与机器人间的网络往返）未纳入处理，仅在测量中含这些延迟。
- **架构未优化**：保持基础策略架构不变（如未设计专门针对本体感知的注意力头），可能限制了进一步反应性提升。
- **验证范围有限**：
  - 仅在GR00T-N1.7这一个基础VLA上验证，未在其他流行模型（如π0、OpenVLA）上测试。
  - 真实世界任务数量有限（4个），且每个任务仅20个试验，统计显著性较弱。
- **潜在偏差风险**：
  - 模拟任务状态基，缺少视觉噪声影响，不完全代表真实场景。
  - 遥操作演示的分布与推理时的延迟分布可能不完全一致，训练时使用的延迟随机分布可能与真实延迟分布存在差异。

（完）

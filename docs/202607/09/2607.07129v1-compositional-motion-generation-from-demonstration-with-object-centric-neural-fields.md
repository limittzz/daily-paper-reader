---
title: Compositional Motion Generation from Demonstration with Object-Centric Neural Fields
title_zh: 基于物体中心神经场的演示式组合运动生成
authors: "Ahmet Ercan Tekden, Yasemin Bekiroglu"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07129v1"
tags: ["query:imitation"]
score: 9.0
evidence: 生成式学习从示范中，通过组合建模用于操作
tldr: 机器人学习面临数据效率与泛化挑战，本文提出基于物体中心神经场的组合运动生成框架。通过共享物体级表示连接感知与运动，用潜在条件变形整合规范神经场以建模几何变化，并采用时序混合专家机制组合物体条件运动基元。实验表明，该方法在长时域操作任务中数据效率优于图像基线，且能鲁棒泛化至新场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07129v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 644, \"height\": 829, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 690, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 928, \"height\": 336, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 834, \"height\": 272, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 879, \"height\": 403, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 919, \"height\": 237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 831, \"height\": 439, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 874, \"height\": 417, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 869, \"height\": 597, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 914, \"height\": 458, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07129v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 861, \"height\": 273, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07129v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 905, \"height\": 334, \"label\": \"Table\"}]"
motivation: 现有运动基元方法缺乏视觉结构支撑，而图像方法数据需求大，需结合物体级组合性提升数据效率与泛化能力。
method: 渲染以规范神经场与潜在条件变形构成的物体中心表示，用时序混合专家门控组合物体条件运动基元生成轨迹。
result: 仿真中长时域操作任务训练数据显著少于图像基线，实机实验具噪声鲁棒性且通过语言分割实现类别泛化。
conclusion: 空间-时间组合性使运动生成在视觉结构上系统泛化，支持直接操作3D场景表示。
---

## 摘要
组合性通过将复杂行为组织为更简单元素的组合，使得机器人学习具有可扩展性和数据效率。利用这一原理，我们提出了一种生成式从演示中学习框架，通过共享的物体级表示连接感知和运动，从而实现对机器人行为的组合建模。我们使用集成规范神经场和潜在条件变形的物体中心神经表示来渲染场景，以平滑、一致且可解释的方式捕捉位置和几何变化。对于运动生成，一个时间混合专家（MoE）采用门控机制随时间组合物体条件运动基元，生成完整轨迹。这种时空组合性保持了运动基元的数据效率，同时将运动锚定在视觉结构中，从而能够跨不同场景配置进行系统泛化。在仿真中，所提出的模型成功完成了长时域操作任务，并且所需训练数据远少于其他基于图像的基线方法。真实世界实验进一步证明了该方法对噪声的鲁棒性、通过基于语言的分割模型在类别级别泛化的能力，以及直接在3D场景表示上操作的能力。

## Abstract
Compositionality, by organizing complex behavior as combinations of simpler elements, enables robot learning that is scalable and data efficient. Leveraging this principle, we propose a generative learning-from-demonstration framework that enables compositional modeling of robotic behavior by connecting perception and motion through shared object-level representations. We render scenes from object-centric neural representations that integrate canonical neural fields with latent-conditioned deformations, capturing positional and geometric variations in a smooth, consistent, and interpretable way. For motion generation, a temporal mixture-of-experts (MoE) employs a gating mechanism to combine object-conditioned movement primitives over time, producing complete trajectories. This spatial-temporal compositionality maintains the data efficiency of movement primitives while grounding motion in visual structure, enabling systematic generalization across diverse scene configurations. In simulation, long-horizon manipulation tasks are successfully completed using the proposed model, which requires significantly less training data than other image-based baselines. Real-world experiments further demonstrate the method's robustness to noise, its ability to generalize at the category level through language-based segmentation models, and its capacity to operate directly on 3D scene representations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：现有从演示中学习（LfD）方法面临两难：运动基元（MPs）虽数据高效但依赖手工设计的低维特征，且组合性常需手动指定；而基于图像的方法（如端到端神经网络）虽能自动提取特征，但需要大量数据且牺牲了MPs的简洁性。如何将感知与运动通过共享的、组合的物体级表示联系起来，在保持数据效率的同时实现系统性的泛化，是本文的核心问题。
- **整体含义**：本文提出一个生成式LfD框架，通过**物体中心的神经场**和**时序混合专家（MoE）**，实现从视觉输入直接生成高质量、可组合的运动轨迹，在极少演示下完成长时域操作任务，并支持类别级泛化。

### 2. 论文提出的方法论

- **核心思想**：将场景建模为多个物体**空间混合专家（Spatial MoE）**的组合，每个物体由一个规范神经场加上潜在条件变形刻画；将运动建模为多个物体条件运动基元在时间轴上的**时序混合专家（Temporal MoE）**，通过软门控权重随时间组合，生成完整轨迹。
- **关键技术细节**：
  - **空间MoE（场景建模）**：
    - 图像预测：\(\hat{I}(x|\{z_i\}) = \sum_i M_i(x_i)F_i(x_i) + M_{bg}F_{bg}(x)\)，其中 \(x_i = x - \Delta_i(x|z_i)\)，\(F_i\) 为物体外观场，\(\Delta_i\) 为潜在条件变形场，\(z_i\) 为物体潜在向量。
    - 物体掩码 \(M_i\) 通过空间 softmax 获得，logit 由 MLP 输出。
    - 训练分三个阶段：背景预训练 → 物体中心训练（含位置感知缩放课程） → 联合组合训练。使用 ℓ2 重构损失和 Lipschitz 正则化。
  - **潜在重标定（Latent Relabeling）**：
    - 对每个物体 \(i\) 构建角落集 \(C_i\)（端点潜在向量），使所有训练演示的潜在向量可表示为角落集的凸组合。
    - 通过算法1（潜在搜索）在测试时从凸组合采样并梯度优化以恢复新场景的潜在向量，保留多个候选。
  - **时序MoE（运动生成）**：
    - 每个物体 \(i\) 的潜在 \( \hat{z}_i\) 通过 FiLM 生成调制参数 \(\{ \gamma_i^{(\ell)}, \beta_i^{(\ell)} \}\)。
    - 时间 t 的聚合条件信号：\( \gamma^{(\ell)}(t) = \sum_i w_i(t) \gamma_i^{(\ell)} \)，\( \beta^{(\ell)}(t) = \sum_i w_i(t) \beta_i^{(\ell)} \)，其中 \( w_i(t) = \text{softmax}(s_i(t)) \) 为可学习的门控权重。
    - 轨迹预测为高斯分布：\( q(t) \sim \mathcal{N}(\mu(t), \text{diag}(\sigma^2(t))) \)，通过 MLP 从聚合条件输出均值与方差。
    - 训练损失为负对数似然，加上早期潜在 dropout 正则化。
  - **姿态建模**：用轴角表示在初始四元数的切空间上连续建模方向。
- **算法流程**（文字描述）：
  - **训练**：给定演示图像、粗物体掩码和轨迹 → 训练空间MoE获得每个物体潜在向量 → 通过潜在重标定将其规范化为角落集凸组合 → 训练时序MoE（门控+FiLM聚合+高斯轨迹生成）。
  - **推理**：给新任务图像 → 对每个物体用算法1搜索潜在向量（凸组合采样+梯度优化） → 送入时序MoE生成完整轨迹。

### 3. 实验设计

- **数据集/场景**：
  - **仿真**：4个操作任务（难度递增）：Wall Avoidance（容易）、Incline Pick-and-Place（中等）、Cup Stacking（中等）、Cube Stacking（困难）。场景包含物体位置、几何变化等变化。
  - **真实世界**：4个机器人任务（均6自由度）：① 拾取桌面物体放入碗中；② 拾取球放入容器（类别泛化）；③ 拾取杯子放到目标点（类别泛化）；④ 将纸箱放入抽屉（3D场景表示）。演示通过运动教学或规划获得。
- **Benchmark**：对比方法包括：
  - CNN-CNMP（条件神经运动基元 + CNN编码器）
  - Diffusion Policy (DP)
  - CNN-FiLM（全局CNN特征 + FiLM轨迹条件）
  - DINO-FiLM（预训练DINO特征 + FiLM）
  - Ours w/o gating（移除时间门控）
  - NFMP（基于神经场的运动基元，仅在Wall Avoidance任务上可比较）
- **数据量**：每个任务设低数据/高数据两个 regime（如Wall Avoidance：10/30演示；Cube Stacking：30/100演示）。本文方法仅在低数据 regime 下训练。

### 4. 资源与算力

- **明确提及**：推理时使用 **Intel(R) Xeon(R) W-2245 CPU @ 3.90GHz** 和 **NVIDIA Quadro RTX 6000 GPU**。推理时间约 **1.4秒/物体（RGB）** 和 **0.8秒/物体（3D）**。
- **未明确**：训练阶段使用的GPU数量、总训练时长、内存等。论文未给出详细计算资源开销。

### 5. 实验数量与充分性

- **仿真实验**：每个任务使用 **3个随机种子** 训练，报告均值和标准差。低数据和高数据 regime 均覆盖。共4个任务、多种基线，形成约24个条件组合（4任务×6方法×2数据量，部分方法无全数据）。消融实验包括“Ours w/o gating”和“Ours w/ MedianBg”（背景建模替代方案）。图6展示数据量‑误差曲线。
- **真实实验**：任务①在25个测试案例（含5个干扰物）上评估，成功率100% (30演示) 或60% (20演示)。任务②③各15个测试案例（含未见过的类别），成功率分别为14/15和15/15。任务④在10个新配置上评估，成功率100%。还进行了多物体语言提示下的测试（5次×3执行）。
- **充分性**：实验覆盖了从简单到困难、仿真到真实、RGB到3D、同类别到类别泛化、有无干扰物等多种设置。消融设计较好（门控、背景建模）。但真实实验的测试样本量偏少（每任务不超过25次），统计显著性有限。仿真实验的随机种子数（3）也较少。总体而言实验设计较客观，但可进一步扩大规模以增强结论稳健性。

### 6. 论文的主要结论与发现

- **数据效率显著**：所提方法在低数据 regime下（如Cube Stacking用30演示）即达到高成功率（0.97），而所有基线即使在更高数据量下也远低于此（DP用100演示仅0.11）。图6曲线明确显示误差随训练量下降最快。
- **时空组合性有效**：去除时间门控后性能下降，证明物体级门控对长时域任务至关重要。
- **场景表示关键**：物体中心神经场相比全局CNN/DINO特征提供更紧凑且泛化能力更强的表示。
- **真实世界鲁棒**：能处理噪声、光照变化、视觉干扰物，并通过语言分割模型实现类别级泛化（如从未见过的棒球、马克杯、喷水壶）。
- **3D表示可行**：在抽屉放置任务中，仅用4个3D占用场演示即泛化到新配置，位置误差在0.3–0.4 cm。

### 7. 优点

- **数据高效**：仅需10–30个演示训练场景模型和运动模型，远少于对比方法。
- **自然的组合性**：物体空间成分解和时序门控均自动学习，无需手工定义子技能或分割点。
- **可解释性**：物体潜在向量直观对应位置/几何变化；门控权重曲线可解释不同物体对轨迹各阶段的影响。
- **灵活性与可扩展**：支持RGB图像和3D占用场两种输入；与语言分割模型集成实现类别泛化；能处理不完美掩码（粗矩形即可）。
- **鲁棒性**：对轻微时间错位、少量掩码缺失、视觉干扰物均有容忍度。

### 8. 不足与局限

- **时间对齐假设**：假定演示轨迹已大致按阶段对齐（如通过接触事件）；严重的时间错位会降低性能（文中报告MED升至2.03，准确率降至0.90）。
- **泛化边界受限**：潜在重标定将泛化局限于训练演示的角落集凸组合内；对超出演示范围的新物体位置/形状可能出现退化（如棒球因硬表面导致抓取失败）。
- **依赖较精确的角落集估计**：角落集通过迭代扩张获得，若训练演示中物体变化范围覆盖不全，模型可能无法处理新场景。
- **掩码需求**：虽然允许粗掩码，但仍需为每个物体在每张训练图像中提供至少矩形标注，增加了标注成本。
- **视觉混淆问题**：当干扰物与任务物体视觉相似时，潜在搜索可能产生歧义，导致重构误差或门控错误。
- **实验规模有限**：真实世界每种任务仅测试15–25次，缺乏大规模统计检验；仿真任务仅3个随机种子，可能不足以完全反映方差。
- **计算资源未充分报告**：训练时间、GPU数量等未公开，不利于复现对比。

（完）

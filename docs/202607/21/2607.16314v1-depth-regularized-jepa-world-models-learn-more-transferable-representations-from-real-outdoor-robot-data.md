---
title: Depth-Regularized JEPA World Models Learn More Transferable Representations from Real Outdoor Robot Data
title_zh: 深度正则化的JEPA世界模型从真实户外机器人数据中学习更多可迁移的表征
authors: Usman M. Khan
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16314v1"
tags: ["query:imitation"]
score: 8.0
evidence: 基于JEPA的世界模型，使用深度正则化进行机器人表示学习
tldr: "针对户外复杂场景中视觉世界模型学习鲁棒潜动态的难题，提出在JEPA训练中引入深度几何先验，结合各向同性潜正则器SIGReg和训练时过参数化，使18M参数的模型在真实农业机器人视频上训练。相比基线LeWM，视觉里程计探针误差降低33%，域内外异常检测惊喜分分离度显著提升，多步潜展开保真度在域偏移下增益随步长增加。结果证明轻量几何先验增强紧凑JEPA模型的表征可迁移性，且不增加推理开销。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16314v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 1150, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1707, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16314v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 1449, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16314v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 717, \"label\": \"Table\"}]"
motivation: 户外真实机器人视频数据视觉复杂，现有JEPA世界模型难以学习鲁棒潜动态。
method: 在JEPA训练中加入深度监督与各向同性潜正则器SIGReg，并增加训练时过参数化。
result: "视觉里程计探针误差降33%，惊喜分分离度提升，多步潜展开保真度增益随步长增长。"
conclusion: 深度作为几何先验可提升紧凑JEPA世界模型在真实户外数据上的泛化性与迁移性。
---

## 摘要
世界模型，特别是基于JEPA架构的模型，已被证明能够学习各种环境的鲁棒动力学。然而，从视觉复杂的真实世界数据中学习仍然是一个挑战，尤其是在不可预测的户外环境中。我们引入深度作为训练时的几何先验，直接从机器人视频数据中学习更鲁棒的潜在动力学并处理视觉复杂性。这将深度监督与各向异性诱导潜在正则化器(SIGReg)相结合，在约束多样性组织方式的同时最大化任务无关的潜在多样性，其组合目标针对与场景几何一致的最高熵表示。为了在不增加推理时间的情况下满足这种更高的复杂性，我们还添加了仅训练时的过参数化。在真实农业机器人视频上训练一个18M参数的模型，我们使用冻结表征的视觉里程计探针、基于预测器的惊喜检测和多步潜在展开保真度进行评估。与基线LeWM相比，我们的方法将视觉里程计探针误差降低了33%，显着提高了域内和域外TartanGround基准上的惊喜分数分离度，并提高了域偏移下的多步展开保真度，且增益随展开步长增加而增大。值得注意的是，我们在与3D几何并非直接相关的物理理解（如光照和阴影）上也看到了惊喜分数分离度的改进。这些结果表明，轻量级的训练时几何先验使紧凑的JEPA世界模型在具有强大潜在表征的真实户外数据上更加有用和可迁移，且不增加推理开销。我们的工作表明，深度作为物理基础的先验可以增强世界模型在各种任务上的泛化能力。

## Abstract
World models, especially based on JEPA architectures, have been shown to learn robust dynamics of various environments. However, learning from visually complex real-world data remains a challenge, especially in unpredictable outdoor environments. We introduce depth as a geometric prior during training in learning more robust latent dynamics directly from robot video data and handling visual complexity. This combines depth supervision with an isotropy-inducing latent regularizer (SIGReg), maximizing task-agnostic latent diversity while constraining how that diversity is organized, with the combined objective targeting the highest-entropy representation consistent with scene geometry. To satisfy this greater complexity without increasing inference time, we also add training-only overparameterization. Training an 18M-parameter model on video from a real agricultural robot, we evaluate with frozen-representation visual odometry probes, predictor-based surprise detection, and multi-step latent rollout fidelity. Compared to the baseline LeWM, our method lowers visual odometry probe error by 33%, substantially increases surprise-score separation both in-domain and on the out-of-domain TartanGround benchmark, and improves multi-step rollout fidelity under domain shift, with gains that grow with rollout horizon. Notably, we also see improvements in surprise-score separation on physics understanding that is not directly tied to 3D geometry, such as lighting and shadows. These results show that a lightweight training-time geometric prior makes a compact JEPA world model more useful and more transferable on real outdoor data with strong underlying representations, without adding inference overhead. Our work suggests that depth as a physically grounded prior can enhance world model generalization on a variety of tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

**核心问题**：在视觉复杂的真实户外环境中（如农业机器人、光照变化、阴影、运动模糊、植被杂乱等），现有JEPA（联合嵌入预测架构）世界模型难以学习鲁棒的潜在动力学。虽然JEPA架构通过预测潜在表征而非像素，能避免建模与任务无关的噪声，但在户外数据上仍然面临表征坍塌和泛化不足的问题。

**研究动机**：  
- 户外机器人（如农用机器人）必须应对动态光照、地形变化、外观突变等挑战，像素级重建模型易被干扰。
- LeWorldModel（LeWM）通过各向同性正则化（SIGReg）避免了坍塌，但仅在模拟环境中验证，尚未在真实户外数据上测试。
- 引入深度作为几何先验，可以引导潜在表征关注物理场景结构，提升表征的可迁移性和鲁棒性。

**整体含义**：  
本文证明，在训练时通过轻量级深度对齐损失，可以让紧凑的JEPA世界模型在真实户外数据上学习到更优的潜在表征，且不增加推理开销，显著提升视觉里程计、异常检测、多步预测的域外泛化能力。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 在LeWM的JEPA训练框架上，额外添加一个**仅训练时使用的深度正则化损失**，迫使RGB嵌入与对应深度相机计算的嵌入对齐（stop-gradient）。
- 同时保留LeWM原有的**SIGReg各向同性正则化**，最大化潜在表征的多样性。
- 为补偿深度正则化带来的额外学习难度，引入**训练时过参数化**（predictor-side overparameterization），通过低秩分支增加预测器容量，训练后可合并回基础权重，不改变推理模型。

### 关键技术细节
1. **网络结构**：ViT-Tiny编码器，嵌入维度192，历史帧数3，预测步长1。预测器为6层Transformer。
2. **损失函数**：  
   \( \mathcal{L} = \mathcal{L}_{\text{pred}} + 0.09 \mathcal{L}_{\text{sigreg}} + 0.1 \mathcal{L}_{\text{depth}} \)
   - \(\mathcal{L}_{\text{pred}}\)：标准潜在预测损失（预测下一个嵌入与真实编码的差距）
   - \(\mathcal{L}_{\text{sigreg}}\)：SIGReg正则化（推动聚集嵌入接近各向同性高斯）
   - \(\mathcal{L}_{\text{depth}}\)：深度对齐损失（余弦相似度损失，深度分支stop-gradient）
3. **训练流程**：  
   - 每个时间步，RGB图像和对应深度图（截断至1米，灰度复制为3通道）通过共享编码器-投影器。
   - RGB嵌入与深度嵌入对齐；深度嵌入仅作监督信号，不参与梯度反向传播。
   - 预测器通过历史RGB嵌入和动作预测下一时刻嵌入。
   - 训练时预测器增加多个rank-16的低秩线性分支（RepLinear），训练后合并。
4. **推理时**：仅用RGB输入，无深度需求，预测器为原架构。

### 公式说明
- 深度对齐损失：\( \mathcal{L}_{\text{depth}} = 1 - \cos(f_\theta(x_{\text{RGB}}), \text{sg}[f_\theta(x_{\text{depth}})]) \)
- 总损失如上方所示，权重比例通过启发式设定（与SIGReg量级相当）。

## 3. 实验设计：数据集 / 场景、benchmark、对比方法

### 数据集
- **训练集**：Aigen Element农业机器人的真实驾驶视频，包含85个片段，约31K帧，来自前向导航立体相机。
- **验证集**：9个片段，3,587帧（Aigen-val）。
- **域外测试集**：TartanGround [14]，一个地面机器人基准数据集，包含6个片段，15,608帧，跨越多个环境（sim-to-real交叉域）。

### 评估任务与Benchmark
1. **视觉里程计（VO）线性探针**：从冻结的嵌入中回归自我运动（ego-motion），评估表征质量。
2. **基于预测器的惊喜检测**：构造合成扰动序列（传送、时间反转、亮度跳变、颜色交换），计算预测误差作为“惊喜分数”，评估模型是否对物理上不合理的事件更敏感。指标包括分类准确率和惊喜分数分离度。
3. **多步潜在展开保真度**：给定真实动作序列，自回归展开预测步长\(h=10\)和\(h=20\)，计算最终步目标相似度和轨迹相似度（余弦相似度）。

### 对比方法
- 主对比：**Vanilla LeWM**（无深度正则化） vs. **Ours**（深度正则化 + 过参数化）。
- 额外参考：**Frozen DINOv2**（一个大规模预训练的单帧视觉编码器），仅用于VO探针和展开保真度对比。

## 4. 资源与算力

论文**未明确说明**具体的GPU型号、数量或训练时长。仅提及训练100个epoch，使用相同优化器和配置。未提供计算资源细节（如是否单GPU、批量大小等）。  
因此，无法量化算力消耗，但模型参数为18M，属于紧凑模型，推测训练成本相对较低。

## 5. 实验数量与充分性

### 实验数量
- 主要实验结果（表1）包含：VO探针（2个方法+1个参考）、惊喜分离度（4种扰动，2个数据集）、展开保真度（2个步长，2个数据集）。
- 额外的分析：图3提供了潜在几何可视化（PCA子空间重叠、主角度、分离度量化）。
- 消融实验：作者声明“隔离过参数化的贡献留给未来工作”，因此**没有明确分离深度正则化和过参数化的单独贡献**。仅通过结果对比整体方案与基线。
- 重复性：作者提到“重复训练获得一致结果”，但未展示多次运行统计量（如方差）。

### 充分性评估
- **正面**：覆盖了三个核心评估维度（表征质量、异常检测、动力学保真度），且包含域内和域外测试。
- **不足**：
  - 缺少消融实验：未单独测试“仅深度正则化无过参数化”和“仅过参数化无深度正则化”。
  - 基线对比单一：仅对比LeWM一个变体，未与V-JEPA、Dreamer等更大模型对比。
  - 数据集规模较小（31K帧），且仅一个机器人和一个农业场景，泛化性有限。
  - 无闭环机器人实验，所有评估为离线指标。
- **客观性**：实验设计合理，但结论中对过参数化的作用需要更严格论证。

## 6. 论文的主要结论与发现

1. **VO探针误差降低33%**：深度正则化使冻结嵌入在自我运动回归上误差从0.0022降至0.0015，优于DINOv2 (0.0029)。
2. **惊喜分离度显著提升**：
   - 域内Aigen-val：分离度从0.067提升至0.120；域外TartanGround：从0.035提升至0.099（近3倍）。
   - 对非几何扰动（亮度跳变、颜色交换）也有改进，说明深度先验帮助形成更强的物理一致性期望。
3. **多步展开在域外更鲁棒**：
   - 域内Aigen-val上深度模型稍差（可能是过拟合减弱）；域外TartanGround上，深度模型在\(h=10\)时目标相似度0.745 vs 0.716，在\(h=20\)时0.537 vs 0.458，优势随步长扩大。
4. **潜在几何分析**：深度正则化使源域和目标域的潜在子空间更对齐（中阶主成分重叠度更高），域间分离度降低（centroid距离从9.96σ降至8.96σ），表明表征更具迁移性。
5. **整体结论**：轻量级训练时的深度几何先验能有效提升紧凑JEPA世界模型在真实户外数据上的表征质量和可迁移性，且不增加推理开销。

## 7. 优点

1. **方法简洁高效**：仅增加一个训练时损失和过参数化，推理模型不变，实用性强。
2. **实验设计有层次**：从表征质量、异常检测、动力学保真度三方面评价，并包含域外泛化测试。
3. **潜在几何可视化**：提供了PCA子空间对齐分析，直观展示了深度正则化如何改善跨域结构。
4. **结果具有启示性**：深度先验甚至改善了非几何物理理解（如光照），表明几何结构能作为组织其他物理知识的骨架。
5. **在紧凑模型上验证**：18M参数即达到显著提升，证明方法不依赖于大规模算力。

## 8. 不足与局限

1. **消融实验不充分**：未单独区分深度正则化和过参数化的各自贡献，难以确定提升主要来源。
2. **基线对比不足**：仅对比LeWM一个变体，未与V-JEPA、DreamerV2、DINO-WM等更广泛的世界模型对比。
3. **数据集规模和多样性有限**：仅一个机器人平台、一个农业场景、31K帧，域外仅一个sim基准（TartanGround），泛化性需更大规模验证。
4. **评估为离线指标**：无闭环机器人实验（如规划、控制），实际部署效果未知。
5. **无统计显著性报告**：未提供多次运行的标准差或置信区间。
6. **深度数据的获取条件**：需要同步的深度相机，虽然仅训练时需要，但在缺乏深度数据的场景下无法直接使用。
7. **过参数化设计未深入探究**：低秩分支的选择（rank=16）是否最优未讨论。

（完）

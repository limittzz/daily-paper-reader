---
title: Distributionally Robust and Safe Imitation Learning
title_zh: 分布鲁棒且安全的模仿学习
authors: "Ahmed Aboudonia, Naira Hovakimyan"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13436v1"
tags: ["query:imitation"]
score: 9.0
evidence: 分布鲁棒且安全的模仿学习
tldr: 模仿学习在复杂决策中表现优异，但分布偏移会引发安全风险。本文提出分布鲁棒安全模仿学习框架，结合泰勒级数模仿学习缓解策略偏移，利用分布鲁棒自适应控制处理不确定性偏移。在无人机规避不安全区域的任务中验证了该方法的有效性和安全性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13436v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 702, \"label\": \"Figure\"}]"
motivation: 现有模仿学习对分布偏移敏感，导致策略在不确定环境下不安全，需要同时考虑策略偏移和不确定性偏移的鲁棒性。
method: 将泰勒级数模仿学习与分布鲁棒自适应控制结合，构建统一优化问题，在分布不确定性下同时优化性能并满足安全约束。
result: 在无人机避障任务中，该方法在不确定环境下成功避免不安全区域，性能优于传统模仿学习。
conclusion: 为安全关键任务的模仿学习提供了分布鲁棒的解决方案，有效平衡性能与安全。
---

## 摘要
模仿学习（IL）在复杂决策任务中取得了显著成功。然而，其性能对分布偏移高度敏感，可能带来重大安全风险。我们提出了一种分布鲁棒且安全的模仿学习框架，显式地处理了策略诱导和不确定性诱导的分布偏移。我们的方法利用泰勒级数模仿学习（TaSIL）和分布鲁棒自适应控制，分别缓解策略诱导偏移和不确定性诱导偏移，从而构建了一个统一框架。该架构使得我们能够提出一个优化问题，在分布不确定性下优化性能，同时系统地考虑安全约束。我们在一项无人机（UAV）案例研究中证明了所提出方法的有效性，其中无人机在不确定环境中执行任务并避开不安全区域。

## Abstract
Imitation learning (IL) has achieved remarkable success in complex decision-making tasks. However, its performance is highly sensitive to distribution shifts, which can pose significant safety risks. We propose a distributionally robust and safe IL framework that explicitly addresses both policy-induced and uncertainty-induced distribution shifts. Our approach develops a unified framework leveraging Taylor Series Imitation Learning (TaSIL) to mitigate policy-induced shifts and distributionally robust adaptive control to handle uncertainty-induced shifts. This architecture enables the formulation of an IL problem that optimizes performance under distributional uncertainty while systematically accounting for safety constraints. We demonstrate the effectiveness of the proposed approach on an unmanned aerial vehicle (UAV) case study where the UAV performs a task in an uncertain environment while avoiding unsafe regions.

---

## 论文详细总结（自动生成）

好的，我将根据您提供的论文内容，按照要求生成一段详细的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：模仿学习（IL）对分布偏移高度敏感，导致其在不确定性环境下性能下降，甚至引发安全问题。分布偏移主要来源于两方面：一是**策略诱导偏移**——学习策略与专家策略的微小偏差随时间累积；二是**不确定性诱导偏移**——环境中的认知不确定性（模型误差）和随机不确定性（噪声）导致状态分布偏移。
- **研究背景**：现有IL方法（如BC、DAgger、TaSIL）主要针对策略诱导偏移进行优化，但忽略了不确定性诱导偏移；而鲁棒IL或分布鲁棒IL方法较少同时处理两种偏移并兼顾安全约束。本文旨在提出一个统一框架，同时缓解两类分布偏移，并在训练中显式考虑安全性。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：采用分层控制架构，将**泰勒级数模仿学习（TaSIL）** 与**L1-分布鲁棒自适应控制（L1-DRAC）** 结合。TaSIL用于减少策略诱导偏移（通过匹配专家策略及其高阶导数）；L1-DRAC用于补偿系统不确定性，保证真实系统状态分布与名义系统状态分布之间的Wasserstein距离有界（即ρL）。基于此，将原始TaSIL问题重新表述为**分布鲁棒安全模仿学习**问题。
- **关键技术细节**：
  - **分布鲁棒TaSIL**：不再仅沿专家轨迹对齐策略，而是考虑一个以专家轨迹状态为中心、半径为ρL的Wasserstein球内的所有分布。损失函数为：  
    `l(ξ,π)=∑_{j=0}^p max_{k} sup_{ν_k∈B_ρL(δ_{x_k(ξ,π*)})} E_{y_k~ν_k} ||∂^j_y π(y_k) - ∂^j_y π*(y_k)||`  
    其中未知的专家策略及其导数通过专家轨迹附近的泰勒展开近似。
  - **安全约束**：引入未来状态在安全集内的机会约束，用CVaR近似，并利用Wasserstein球转化为确定性约束形式：  
    `c_j^T z_k(ξ) - d_j + ||c_j||_2 √(δ_s/n_c) ρL ≤ 0`  
    损失函数中加入对应惩罚项：`λ ∑_k ReLU(Z_k(ξ))`。
- **算法流程**（文字说明）：
  1. 收集专家演示轨迹（确定性名义系统下）。
  2. 使用L1-DRAC参数（ω, Ts, λs）保证闭环系统在不确定性下的Wasserstein距离有界（经验选取ρL）。
  3. 训练策略网络，最小化包含分布鲁棒TaSIL损失和安全惩罚项的期望损失。其中分布鲁棒项通过梯度上升求解内部上确界。
  4. 部署时，策略π与L1-DRAC控制器共同作用。

### 3. 实验设计：使用的场景、基准、对比方法
- **场景**：无人机（UAV）位置跟踪任务，状态12维（位置、速度、欧拉角、角速率），输入4维（推力、滚转/俯仰/偏航力矩）。专家规划器生成半径为0.8m的圆形水平轨迹。安全区域定义为 x≤0.8 且 y≤0.8。
- **不确定性设置**：认知不确定性为作用于期望轨迹通道的随机正弦扰动；随机不确定性为布朗运动。
- **对比方法**：
  - 标准TaSIL（无不确定性）
  - 标准TaSIL（有不确定性）
  - 标准TaSIL + L1-DRAC（有不确定性）
  - 本文提出的分布鲁棒安全TaSIL + L1-DRAC（有不确定性）
- **基准**：未与现有其他分布鲁棒IL方法（如DRIP）定量比较，仅对比上述变体。

### 4. 资源与算力
- **文中未明确说明**：未提及GPU型号、数量、训练时长等算力信息。仅提到策略使用两层32神经元GELU网络，训练1000次迭代，10条专家轨迹（每条150点）。推测算力需求较低，但无法确认具体硬件。

### 5. 实验数量与充分性
- **实验数量**：仅展示了一个UAV场景下的蒙特卡洛模拟结果（图1），对比了四个条件，每组显示了多条轨迹的叠加。没有进行**定量指标**（如平均轨迹误差、安全违反率、成功率）的统计，也没有**消融实验**（例如单独验证分布鲁棒项或安全项的效果）。
- **充分性**：实验数量较少，仅定性验证了可行性，缺乏多场景、多参数、多随机种子的重复实验。对比基线不够全面（缺少与其他分布鲁棒IL方法如DRIP的定量比较）。因此充分性一般，公平性尚可（因各变体条件一致）。

### 6. 论文的主要结论与发现
- 标准TaSIL在无不确定环境下能成功模仿专家；加入不确定性后轨迹显著偏移。
- 添加L1-DRAC可以部分恢复轨迹跟踪性能，但仍可能进入不安全区域。
- 本文提出的分布鲁棒安全TaSIL+L1-DRAC能够在跟踪专家轨迹的同时，在不安全区域附近主动偏离，保证安全约束。
- 理论贡献：将Wasserstein鲁棒性纳入TaSIL并加入安全约束，为安全关键任务提供了可认证的鲁棒性保证。

### 7. 优点
- **方法创新性**：统一处理策略诱导和不确定性诱导两类分布偏移，并首次在IL框架中显式集成安全约束。
- **理论支撑**：基于L1-DRAC的Wasserstein距离有界结果和TaSIL的ISS稳定性分析，为分布鲁棒性提供了理论依据。
- **工程实用性**：采用分层架构，可将TaSIL和L1-DRAC独立设计后联合使用，便于模块化实现。
- **安全处理简洁**：将机会约束转化为可微的惩罚项（ReLU），易于梯度优化。

### 8. 不足与局限
- **实验覆盖不足**：仅在一个UAV场景下验证，缺乏多任务、多模态不确定性、真实系统或硬件的测试。
- **缺乏定量评估**：没有报告数值结果（如平均Wasserstein距离、安全违反率、计算时间），仅提供定性轨迹图，无法严格评估性能。
- **超参数经验性**：ρL复合参数（ρ+ρa）是经验选取的，且L1-DRAC参数（ω, Ts, λs）与策略训练未协同设计，论文指出这是一个未解决的开问题。
- **安全假设简化**：安全约束仅考虑一步预测得到的线性约束，多步安全并未严格保证。
- **计算代价**：分布鲁棒项需要求解内部上确界（梯度上升），可能增加训练时间，但文中未分析。
- **泰勒近似的局限性**：在分布偏移较大时，用专家轨迹附近的泰勒展开近似专家策略可能不准确。

（完）

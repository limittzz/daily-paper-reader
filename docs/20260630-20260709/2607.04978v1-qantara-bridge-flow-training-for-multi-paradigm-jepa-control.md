---
title: "Qantara: Bridge-Flow Training for Multi-Paradigm JEPA Control"
title_zh: Qantara：面向多范式JEPA控制的桥式流训练
authors: "Ruslan Rakhimov, George Bredis, Yuriy Maksyuta, Daniil Gavrilov"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04978v1"
tags: ["query:imitation"]
score: 9.0
evidence: 提出了一种新的JEPA训练目标，支持多种推理范式，包括行为克隆
tldr: "现有JEPA世界模型训练时只能选择单一推理范式（轨迹优化或行为克隆），无法在部署时根据成本或观测条件切换。Qantara提出桥流训练方法，通过布朗桥插值状态潜在和动作流匹配联合训练，使同一检查点支持三种推理范式：潜在规划、行为克隆动作采样和视频逆动力学。在LeWM套件上达到91.2%成功率，OGBench-Cube超越先前最优7.7个百分点，且行为克隆与逆路径也达到82-83%和71-73%成功率。这首次将JEPA从单范式规划器扩展为多范式控制器。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04978v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04978v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1376, \"height\": 1249, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04978v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1381, \"height\": 1241, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04978v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1085, \"height\": 300, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1376, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1270, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1143, \"height\": 206, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04978v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1018, \"height\": 423, \"label\": \"Table\"}]"
motivation: 现有JEPA世界模型训练时承诺单一推理范式，无法在部署时根据约束选择最优范式，需要一种联合训练方法使一个检查点支持多范式推理。
method: 提出桥流训练：用布朗桥插值相邻干净潜在状态，同时用噪声到数据流匹配动作轴，训练时集中采样噪声方图的边缘，使预测器适配三种推理模式。
result: Qantara在LeWM套件达到91.2 SR三种子平均，OGBench-Cube上比DINO-WM高7.7 SR、比LeWM高19.7 SR；同一权重下行为克隆和视频逆路径分别达82-83 SR和71-73 SR。
conclusion: Qantara证明通过桥流训练可使JEPA模型同时支持轨迹优化、行为克隆和逆动力学，将JEPA从单范式规划器提升为多范式控制器，且性能达到新SOTA。
---

## 摘要
联合嵌入预测架构（JEPAs）支撑着日益增长的基于原始像素控制的潜在世界模型家族，但每个现有的JEPA世界模型在训练时都承诺采用单一推理范式：要么在学习到的动力学模型中进行轨迹优化，要么直接进行行为克隆。一个同时服务于两者的单个检查点会将这一选择推迟到推理阶段，此时部署约束（展开成本、观测可访问性）决定了哪条路径胜出。我们提出Qantara，一种端到端的JEPA，其联合训练目标将状态轴上连续干净潜在变量之间的布朗桥插值与动作轴上的噪声到数据流匹配相结合。相同的检查点无需重新训练即可服务于三种推理范式：潜在规划、行为克隆动作采样和逆动力学，我们通过一种视频逆组合来查询逆动力学，该组合首先在无动作条件下预测下一个潜在变量，然后提取动作。训练将质量集中在（动作-时间，状态-时间）噪声正方形的边缘上，推理在此处查询预测器：将其替换为均匀内部采样会使Push-T规划在同等计算量下从90.1 SR下降到53.3 SR。在LeWM控制套件上，Qantara达到了三个训练种子的平均91.2 SR，并在OGBench-Cube上设立了新的SOTA（比DINO-WM高7.7 SR，比LeWM高19.7 SR）。来自相同权重的行为克隆和视频逆路径在Push-T上达到82-83 SR，在Cube上达到71-73 SR。这些结果将JEPA世界模型从单一范式规划器转变为多范式控制器。

## Abstract
Joint-Embedding Predictive Architectures (JEPAs) underpin a growing family of latent world models for control from raw pixels, but every existing JEPA world model commits at training time to a single inference paradigm: either trajectory optimisation in a learned dynamics model, or direct behaviour cloning. A single checkpoint that serves both would defer this choice to inference, when deployment constraints (rollout cost, observation accessibility) determine which path wins. We present Qantara, an end-to-end JEPA whose joint training objective pairs a Brownian-bridge interpolant between consecutive clean latents on the state axis with noise-to-data flow matching on the action axis. The same checkpoint serves three inference paradigms without retraining: latent planning, behaviour-cloning action sampling, and inverse dynamics, which we query through a video-inverse composition that first predicts the next latent without action conditioning, then extracts the action. Training concentrates mass on the edges of the (action-time, state-time) noise square, where inference queries the predictor: replacing it with uniform interior sampling drops Push-T planning from 90.1 to 53.3 SR at matched compute. On the LeWM control suite, Qantara reaches a 91.2 SR three-train-seed average and sets new SOTA on OGBench-Cube (+7.7 SR over DINO-WM, +19.7 over LeWM). From the same weights, the behaviour-cloning and video-inverse paths reach 82-83 SR on Push-T and 71-73 SR on Cube. These results move JEPA world models from single-paradigm planners to multi-paradigm controllers.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题背景**：现有的基于联合嵌入预测架构（JEPA）的视觉世界模型（如PLDM、DINO-WM、LeWM）在训练时都预先承诺了单一的推理范式——要么训练成基于学习到的动力学模型进行轨迹优化（planning），要么训练成直接进行行为克隆（behaviour cloning）。但在实际部署时，每步延迟、动作搜索空间大小、能否提供目标观测等约束是训练时无法预料的，一个支持多推理范式的检查点将能根据部署条件灵活选择最优路径。
- **核心问题**：能否训练一个参数量小于十亿的JEPA模型，使同一组权重同时支持潜在规划、行为克隆和逆动力学，且在各范式上都不显著损失性能？
- **整体含义**：填补了“子十亿JEPA × 多推理范式”这一空白，将JEPA从单一范式规划器提升为多范式控制器，实现了在同一检查点下三种推理路径的零重训切换。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用**桥流训练（Bridge-Flow Training）**，在状态轴上使用布朗桥插值（Brownian-bridge interpolant）连接相邻的干净潜在变量，同时在动作轴上使用噪声到数据的流匹配（flow matching）。训练时采用**边缘对齐采样（edge-aligned sampling）**，将(τᵃ, τᶻ)采样集中在噪声正方形的四条边和对角线上，从而让模型在推理时不同路径对应的(τᵃ, τᶻ)组合都能得到充分训练。

- **关键技术细节**：
  - **模型架构**：编码器Eφ: oₜ → zₜ；预测器fθ：块因果自回归，块内双向注意力，联合预测动作aₜ和下一潜在zₜ₊₁。
  - **插值方案**：
    - 动作轴：˜aₜ = τᵃₜ aₜ + (1-τᵃₜ)εᵃ (标准流匹配)
    - 状态轴：˜zₜ₊₁ = (1-τᶻₜ₊₁)zₜ + τᶻₜ₊₁zₜ₊₁ + γ√(τ(1-τ))εᶻ (布朗桥，τ=0时为前一个干净潜在)
  - **训练损失**：L = λz Lᴮ + λa LᵃFM + λSIG SIGReg，其中Lᴮ为状态桥损失，LᵃFM为动作流匹配损失，SIGReg防止编码器坍缩。
  - **边缘对齐采样**：每个batch复制5份，分别从四个边缘（forward、policy、video、inverse）和对角线（joint）采样(τᵃ, τᶻ)，同时保持前缀清洁、后缀单调递减τ序列。
  - **三种推理范式**（同一权重）：
    - **潜在规划**：给定目标潜在z_g，在CEM搜索中使用forward边缘（τᵃ=1, τᶻ:0→1）展开动力学，选择终端潜在最接近z_g的动作序列。
    - **行为克隆采样**：使用policy边缘（τᶻ=0, τᵃ:0→1），以当前潜在为条件，从高斯噪声欧拉采样出干净动作。
    - **视频-逆组合**：先沿video边缘（τᵃ=0, τᶻ:0→1）预测无动作条件下的下一潜在ẑₜ₊₁，再沿inverse边缘（τᶻ=1, τᵃ:0→1）恢复桥接动作。

### 3. 实验设计

- **数据集/场景**：LeWM-suite中的四个环境：
  - Two-Room（2D导航，10k轨迹，平均92步，door-then-target启发式数据）
  - Push-T（2D接触式操控，20k人类专家演示，平均196步，使用DINO-WM发布数据集）
  - OGBench-Cube（3D抓取放置，10k轨迹，200步，脚本化策略数据）
  - Reacher-Hard（精确两关节对齐控制，10k轨迹，200步，SAC专家策略数据）

- **Benchmark**：与三个端到端像素JEPA世界模型基线对比：PLDM、DINO-WM（无本体感知版本）、LeWM。同时使用LeWM的复现结果进行公平比较。

- **对比方法**：PLDM、DINO-WM、LeWM（论文报告版 + 作者复现版）、GCBC（目标条件行为克隆，仅作为Push-T对照）。

### 4. 资源与算力

- **文中未明确说明**所使用的GPU型号、数量或训练时长。仅提到模型参数量约21M，训练10个epoch，batch size为128（由5个模式复制，有效batch=640），优化器为AdamW，峰值学习率3×10⁻⁴，使用bf16混合精度。因此无法报告具体算力消耗。

### 5. 实验数量与充分性

- **主要实验**：在4个环境上，每个(recipe, env)单元统计50 episodes × 5 eval seeds × 3 train seeds = 750 episodes，报告3个训练种子均值的均值与种群标准差。实验设计遵循LeWM的标准三种子协议，并增加了5倍评估种子量。
- **消融实验**：
  - τ采样设计：边缘对齐 vs 均匀采样（表3）
  - 模式集消融：5-mode、4-mode（分别丢弃policy/inverse/video/joint）、3-mode、2-mode、1-mode（表3）
  - 输出残差+零初始化消融（表5）
  - 布朗桥γ参数扫除（0, 0.5, 1, √2, 2，表4）
  - CEM去噪步数K扫除（1,2,4,8，表6）
  - 预测器dropout p=0与p=0.1对比（文中文字提及）
  - 单调τ链 vs i.i.d. τ（附录C）
  - 不同推理路径（CEM、BC、video-inverse）在同一检查点上的性能（表2）
- **充分性评估**：实验覆盖了多个场景（2D/3D、导航/操控/电机控制），消融设计较为系统，重点验证了边缘对齐采样、输出残差、模式集等关键设计。但仅局限于仿真环境，且数据均为近专家离线数据，缺少真实机器人转移和自然视频预训练评估。总体较为充分，但存在场景泛化局限。

### 6. 论文的主要结论与发现

- Qantara在LeWM-suite的规划路径上平均91.2 SR，在OGBench-Cube上达到93.7 SR，比先前SOTA DINO-WM高7.7 SR，比LeWM高19.7 SR。
- 同一检查点下的行为克隆和视频-逆路径在Push-T上分别达到82.1 SR和83.2 SR，在Cube上达到70.8 SR和72.8 SR，推理成本比规划低15-65倍。
- 边缘对齐采样是关键：替换为均匀采样导致Push-T CEM下降36.8 SR（90.1→53.3）。
- 模式集消融发现**角共同正则化效应**：丢弃policy、inverse或joint模式都会使得Push-T训练在所有推理路径上不稳定，即使被丢弃的模式对应的边缘并不用于某些推理路径。这表明五个模式共同正则化共享主干，超出单纯覆盖推理角落的作用。
- 输出残差+零初始化是必要的：去掉后Push-T下降15.2 SR且训练不稳定。

### 7. 优点

- **方法创新**：首次将布朗桥插值与流匹配联合，使得状态轴τ=0自动匹配规划器的输入（前一个干净潜在），无需学习从高斯噪声到潜在流形的映射；边缘对齐采样高度契合推理查询几何，训练计算效率高。
- **范式灵活性**：单一检查点同时支持规划、行为克隆、逆动力学三种推理路径，无需重训，可在部署时根据延迟或目标可用性选择最优方式。
- **性能优异**：在OGBench-Cube上显著超越先前SOTA，参数量仅21M，远小于像素级多范式模型（150M-7B）。
- **消融充分**：多个消融揭示了设计的必要性，特别是角共同正则化这一反直觉发现为未来多范式训练提供了重要指导。

### 8. 不足与局限

- **实验环境限制**：仅包含四个仿真环境，且数据均为近专家离线数据；缺少真实机器人部署验证和自然视频预训练数据。
- **推理步数限制**：所有三种派遣均每步只去噪一个块，未扩展到分块多步推理（如Diffusion Forcing），直接扩展尚待验证。
- **参数量规模局限**：仅展示约21M参数规模，未在更大的JEPA主干（如V-JEPA-2）上测试，缩放行为未知。
- **目标盲推理限制**：行为克隆和视频-逆组合仅在当前观测包含目标信息（如Push-T渲染目标）或容忍度吸收漂移时有效，在Reacher等精确任务中退化为近噪声（6.1/4.8 SR）。
- **规划计算开销**：CEM规划每步需0.4-1.2秒，而BC和视频-逆仅17-24毫秒，延迟敏感场景仍需选择后者。

（完）

---
title: "SiamJEPA: On the Role of Siamese Student Encoders in JEPA"
title_zh: SiamJEPA：关于孪生学生编码器在JEPA中的作用
authors: Makoto Yamada
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.04044v1"
tags: ["query:imitation"]
score: 9.0
evidence: 研究JEPA自我监督学习中孪生学生编码器的作用
tldr: 自监督表示学习中，JEPA模型通常使用单编码器学生网络。本文提出SiamJEPA，采用孪生学生编码器并搭配EMA教师网络。实验表明，孪生编码器作为有效正则化，提升表示分离性并加速早期训练，在有限预算下优于单编码器JEPA，线性探测精度高于MAE。该工作揭示了孪生学生结构对预测表示学习的重要性，提供了简单有效的改进方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04044v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1283, \"height\": 588, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04044v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1303, \"height\": 473, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04044v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1019, \"height\": 583, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 900, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1091, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 972, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1262, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1237, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04044v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1225, \"height\": 282, \"label\": \"Table\"}]"
motivation: 探索孪生学生编码器在JEPA中的作用，因其更符合脑启发学习但尚未被充分研究。
method: 提出SiamJEPA，采用掩码孪生学生编码器与指数移动平均教师网络，可视为PhiNet的JEPA形式。
result: 在ImageNet线性探测中，SiamJEPA加速早期学习、提高表示分离性，且有限预算下优于单编码器JEPA和MAE。
conclusion: 孪生学生编码器是预测表示学习的重要归纳偏置，为改进自监督学习提供简单有效方法。
---

## 摘要
近期，联合嵌入预测架构（JEPA）作为自监督表示学习的一个有前景框架，引起了计算机视觉和机器学习社区的广泛关注。与重建像素的掩码自编码器不同，JEPA模型通过预测掩码区域的潜在嵌入来学习表示。现有基于JEPA的方法（如I-JEPA和V-JEPA）通常在学生网络中使用单一编码器。相比之下，为学生网络使用孪生编码器更自然地符合脑启发的表示学习框架，但它们在JEPA模型中的作用仍未被充分探索。本文研究了孪生学生编码器在基于JEPA的表示学习中的效果。为此，我们提出了SiamJEPA，即配备指数移动平均（EMA）教师网络的掩码孪生学生编码器。SiamJEPA也可被视为脑启发表示学习模型PhiNet的JEPA形式。通过在ImageNet线性探测上的广泛实验，我们证明孪生编码器作为JEPA目标的有效正则化器，能提高表示的可分离性并在训练早期加速学习。此外，在有限训练预算下，SiamJEPA始终优于可比的单编码器JEPA变体，并达到比需要更长时间训练的掩码自编码器（MAE）更高的线性探测准确率。我们的发现表明，孪生学生编码器不仅是架构选择，更构成预测性表示学习的重要归纳偏置。这些结果为基于JEPA的模型设计提供了新见解，并表明引入孪生学生架构为改进自监督表示学习提供了一种简单而有效的方法。

## Abstract
Recently, Joint Embedding Predictive Architectures (JEPAs) have attracted significant attention in the computer vision and machine learning communities as a promising framework for self-supervised representation learning. Unlike masked autoencoders that reconstruct pixels, JEPA models learn representations by predicting latent embeddings of masked regions. Existing JEPA-based methods, such as I-JEPA and V-JEPA, typically employ a single encoder in the student network. In contrast, using Siamese encoders for student network is more naturally aligned with brain-inspired representation learning frameworks, yet their role in JEPA models remains largely unexplored. In this paper, we investigate the effect of Siamese student encoders in JEPA-based representation learning. To this end, we propose SiamJEPA, masked Siamese student encoders equipped with an exponential moving average (EMA) teacher network. SiamJEPA can also be viewed as a JEPA formulation of the brain-inspired representation learning model PhiNet. Through extensive experiments on ImageNet linear probing, we demonstrate that Siamese encoders act as an effective regularizer for the JEPA objective, improving representation separability and accelerating learning during the early stages of training. Furthermore, SiamJEPA consistently outperforms comparable single-encoder JEPA variants under limited training budgets and achieves higher linear probing accuracy than Masked Autoencoders (MAE) which requires longer training. Our findings reveal that Siamese student encoders are not merely an architectural choice but constitute an important inductive bias for predictive representation learning. These results provide new insights into the design of JEPA-based models and suggest that incorporating Siamese student architectures offers a simple yet effective approach for improving self-supervised representation learning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：近期，联合嵌入预测架构（JEPA）成为自监督表示学习的重要框架（如 I-JEPA、V-JEPA），但这些方法在学生网络中均使用单一编码器。相比之下，脑启发的表示学习模型（如 PhiNet）采用**孪生学生编码器**，更符合生物学习机制（如 temporal prediction hypothesis 和 complementary learning systems 理论）。然而，孪生学生编码器在 JEPA 框架中的作用尚未被系统研究。
- **核心问题**：**孪生学生编码器在 JEPA 中扮演什么角色？** 它能否带来更好的表示质量与训练效率？
- **整体含义**：通过提出 SiamJEPA 并系统分析，论文揭示了孪生学生编码器不仅是架构选择，更是**预测性表示学习的重要归纳偏置**，能有效正则化表示空间、加速早期收敛，并提升有限预算下的性能。

### 2. 论文提出的方法论：核心思想、关键技术细节与流程
- **核心思想**：在 JEPA 框架中引入**两个独立的孪生学生编码器**（共享权重），分别处理两个非重叠的掩码视图，并利用 EMA 教师网络生成目标表示。通过两个互补目标进行训练：
  1. **Sim-1**：用 KL 散度对齐两个孪生学生编码器的全局表示（通过后验-先验概率建模）。
  2. **Sim-2**：用归一化均方误差（NMSE）预测被掩码 patch 的潜在表示。
- **关键技术细节**：
  - **架构**：两个学生编码器 f（参数共享）、一个 EMA 教师编码器 f_ema、两个预测器 h（线性层，对齐全局表示）和 g（Transformer，预测掩码 patch）。
  - **掩码策略**：两个视图的掩码集合严格不重叠（M1 ∩ M2 = ∅），防止捷径学习。
  - **损失函数**：最终损失 L = 0.5 × (MSE^(1) + MSE^(2)) + λ_KL × 0.5 × (KL_sg^(1) + KL_sg^(2))。其中 KL 项使用 stop-gradient 作用于先验分支，提高稳定性；λ_KL 控制孪生正则化强度，λ_KL=0 退化为类似单编码器 JEPA。
  - **概率预测器**：g 采用后验/先验分布建模（q 和 p 均为两层 MLP），类似于 Denton & Fergus 的随机视频预测方法，使模型能捕捉预测的不确定性。
- **算法流程**（文字说明）：
  - 输入图像 X，生成两个非重叠掩码视图 Mask(X, M1) 和 Mask(X, M2)。
  - 通过共享权重学生编码器 f 得到 H^(1) 和 H^(2)，通过 EMA 教师编码器得到 Y。
  - 通过线性预测器 h 对齐 Hcls，通过 KL 损失（Sim-1）促使单一视图的先验接近双视图的后验。
  - 通过预测器 g 从经 h 变换的全局表示和潜在变量 z 生成预测表示，与教师 Y 的掩码区域计算 NMSE（Sim-2）。
  - 联合优化两个损失。

### 3. 实验设计：数据集、基准与对比方法
- **数据集**：ImageNet-1K（1000 类分类）。
- **基准任务**：**线性探测（Linear Probing）**——在冻结的预训练表示上训练线性分类器，报告 Top-1 准确率。
- **对比方法**：
  - MAE（Masked Autoencoders）——基于像素重建，训练 400 和 1600 epoch。
  - CAE（Context Autoencoder）——同样基于重建，训练 1600 epoch。
  - I-JEPA——原始 JEPA 方法（单学生编码器），训练 600 epoch（结果引自原文，非直接对比）。
  - SiamJEPA 自身在不同超参数配置下的变体。
- **消融实验**：覆盖以下因素：
  - KL 正则化权重 λ_KL（0.0001 / 0.01 / 0.03）
  - free-bit 阈值（0.01 / 0.05 / 0.1）
  - 掩码策略（随机掩码 vs 块掩码）及掩码比例（0.7 / 0.75 / 0.8）
  - 权重衰减（0.05 / 0.1）
  - 预测目标（CLS token / 第10层 mean pooling / 第12层 mean pooling）
  - 预测器深度（简要提及，未全面报告）
- **实验设置**：ViT-Base 骨干，有效批大小 8192，训练 400 epoch（消融常用 300 epoch 以节省算力），优化器 AdamW，学习率 1.5e-4。

### 4. 资源与算力
- 论文明确使用的 GPU：**NVIDIA V100、A100 或 H100**。
- 有效批大小：8192，训练 400 epoch。
- 未给出具体 GPU 数量、训练总耗时或 GPU 小时数。作者也指出受计算资源限制，部分消融（如预测器深度）未完全深入。

### 5. 实验数量与充分性
- **实验数量**：
  - 主要性能对比（表2）：SiamJEPA vs MAE、CAE、I-JEPA。
  - 消融实验共 6 张表格和 2 张图：
    - 表3：λ_KL 影响（3 种值 × 4 个 epoch 点）
    - 表4：free-bit 影响（3 种值 × 4 个 epoch 点）
    - 表5：掩码策略（随机/块 × 3 种掩码比例 × 4 个 epoch 点）
    - 表6：权重衰减（2 种值 × 5 个 epoch 点）
    - 表7：预测目标（3 种目标 × 4 个 epoch 点）
    - 图2：学习曲线（λ_KL 大小对比）
- **充分性与公平性**：
  - **优点**：消融设计系统，覆盖了关键超参数和设计选择；使用相同的代码库和实验设置进行公平对比。
  - **不足**：
    - 与 I-JEPA 的比较非直接（不同 epoch、不同实现），作者坦诚说明。
    - 仅在 ViT-Base 上测试，未扩展到更大模型。
    - 预测器深度的实验因资源限制未完整呈现。
    - 线性探测仅反映表示质量的一个侧面，未在下游任务（如检测、分割）上验证。

### 6. 论文的主要结论与发现
- 孪生学生编码器在 JEPA 中作为**有效正则化器**：提高表示的可分离性（更 discriminative），并加速早期训练收敛。
- SiamJEPA（400 epoch）在 ImageNet 线性探测上达到 **70.2% Top-1**，优于 MAE 400 epoch（61.9%）和 MAE 1600 epoch（68.0%），训练效率显著更高。
- **块掩码（block masking）** 策略对 SiamJEPA 至关重要，与 I-JEPA 观察一致。
- KL 正则化并非主要学习信号（Sim-2 才是），但提供了有益的早期归纳偏置；λ_KL 需要适当平衡（0.01 较好）。
- 中间层 mean pooling（第10层）优于 CLS token 或末层 pooling，表明语义信息分散在 patch 表示中。
- 较大的权重衰减（0.1）在长训练中更有利。

### 7. 优点
- **问题新颖**：首次系统研究孪生学生编码器在 JEPA 中的作用，填补了理论空白。
- **统一框架**：SiamJEPA 可视为 PhiNet 的 JEPA 形式，并通过 λ_KL 连续控制孪生影响，便于分析。
- **实验设计合理**：消融实验全面，覆盖核心超参数；使用相同的代码库与设置，结果可靠。
- **训练效率突出**：在明显更少的 epoch 下超越 MAE，显示潜在预测的优越性。
- **可解释性强**：分析了 KL 项作为正则化的角色，以及掩码策略、权重衰减等的影响，提供实践指导。

### 8. 不足与局限
- **模型规模限制**：只在 ViT-Base 上实验，未验证在 ViT-L/H 上的可扩展性。
- **对比不够直接**：I-JEPA 的引用结果来自不同训练设置（600 epoch），不是公平比较。
- **任务覆盖窄**：仅用线性探测评估，未在目标检测、语义分割等下游任务上验证表示泛化性。
- **超参数敏感**：发现最终性能对实现细节敏感，但未提供完善的超参数自动搜索。
- **计算资源不足**：预测器深度等消融未彻底完成，部分结论可能受限于实验规模。
- **理论分析不足**：虽然观察了正则化效果，但未从理论上解释为什么孪生结构有效。

（完）
